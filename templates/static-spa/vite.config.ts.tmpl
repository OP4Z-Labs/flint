/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { VitePWA } from 'vite-plugin-pwa'

// {{appName}} is a fully static SPA — no Pages Functions, no backend.
// wrangler is here only for `wrangler pages deploy`. Vitest gets its own
// config in vitest.config.ts.
//
// PWA: the plugin owns the manifest so it stays in sync with the precache
// list. The static-spa variant uses precache-only (no runtime caching) —
// that's the safe default. Add runtime caching rules in `workbox` only
// when you know which third-party origins your page actually loads.

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      injectRegister: 'auto',
      includeAssets: ['favicon.svg', 'og-image.png'],
      manifest: {
        name: '{{appName}}',
        short_name: '{{appName}}',
        description: '{{appName}} — generated by Flint.',
        start_url: '/',
        scope: '/',
        display: 'standalone',
        background_color: '#0a0d10',
        theme_color: '#0a0d10',
        icons: [
          { src: '/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
          { src: '/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'any' },
          { src: '/icon-maskable-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
        ],
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,svg,png,ico,woff,woff2}'],
        navigateFallback: '/index.html',
      },
      devOptions: { enabled: false },
    }),
  ],
  resolve: {
    alias: { '@': '/src' },
  },
})
